services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: streamcut
      POSTGRES_USER: streamcut
      POSTGRES_PASSWORD: streamcut
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U streamcut -d streamcut"]
      interval: 5s
      timeout: 5s
      retries: 12
